# This directory contains raw source documents (PDFs, articles, etc.)
# These files are immutable — never modify them after placing them here.
# Drop your documents here, then ask Gemini to process them.
